/**
 * Plain (non-module) stylesheet side-effect imports — `import '@hanzo/ui/theme.css'`.
 * Next.js only declares `*.module.css`, so the bare path has no declaration of its own.
 */
declare module '*.css'
